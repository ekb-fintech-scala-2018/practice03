package fintech.lesson03.exercises

import fintech.lesson03.examples.Example04.{Empty, Linked}

object Exercise01 {
  // Напишите тип Maybe[T].
  // Maybe[T] может быть представлен либо классом Just[T] содержащим значение x типа T,
  // либо Missing[T] олицетворяющим отсутствие объекта

  // Напишите функцию foreach и count для Maybe[T]
  // Напишите функцию oneLevel[T](listOfMaybes: Linked[Maybe[T]]): Linked[T]


  def main(args: Array[String]): Unit = {
//    val maybes: Linked[Maybe[Int]] =
//      new Just(1) :: new Missing[Int] :: new Just(3) :: new Empty[Maybe[Int]]
//
//    println(maybes)
//    println(oneLevel(maybes))
  }
}
