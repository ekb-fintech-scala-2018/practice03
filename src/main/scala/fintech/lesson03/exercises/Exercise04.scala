package fintech.lesson03.exercises

import fintech.lesson03.examples.Example06.{Circle, Shape, Square}
import fintech.lesson03.examples.Example10.{Empty, Linked}
//import fintech.lesson03.exercises.Exercise02.{Just, Maybe}

object Exercise04 {
  // Напишите интерфейс Iterator[F[_]]. Iterator должен содержать метод "foreach"
  // который позволяет вызывать переданную функцию f на всех елементах внутри F
  // Реализуйте интерфейс для типов Example10.Linked и Exercise02.Maybe
  // Напишите метод printAllShapes[F[_]](shapes: F[Shapes], iterator: Iterator[F]): Unit
  // Который будет выводить информацию о всех фигурах из еще неизвестной коллекции F


  def main(args: Array[String]): Unit = {
//    val listOfShapes = new Circle(1) :: new Square(2) :: Empty
//    val maybeACircle = new Just(new Circle(1))
//
//    printAllShapes(listOfShapes, listIterator)
//    printAllShapes[Maybe](maybeACircle, maybeIterator)
  }

}
